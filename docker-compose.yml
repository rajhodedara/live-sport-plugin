version: '3.8'

services:
  nuvio:
    build: .
    container_name: nuvio-live-sports
    ports:
      - "7000:7000"
    env_file:
      - .env
    environment:
      - PORT=7000
    restart: unless-stopped
